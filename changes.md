# Change Staging

Use this file as a release-note staging area before version bumps and zip builds.
Add one row per shipped change so changelog text and reviewer notes are ready.

| Date | Area | Change | Reviewer Notes |
| --- | --- | --- | --- |
| 2026-07-14 | CampusNet Files Overview | Darkened the legacy file-list header containing Name, Author, and Last modified without broadening CampusNet table styling. The correction is limited to the Files Overview route and clears itself in light mode. | Open Personal Files / Files Overview in dark mode. Confirm the Name, Author, and Last modified header is dark 2 with readable text, hover a file row, then turn Dark Mode off and confirm the native header returns. |
| 2026-07-14 | CampusNet My Links | Removed the unintended dark-2 strips behind the plain Personal links and Group links page headings, so those structural headings now merge with the dark-1 canvas while the actual group-links table header remains a raised data surface. | Open My Links in dark mode and confirm Personal links and Group links have no full-width grey fill, while the Name/Description table header remains distinct and links are readable. |
| 2026-07-14 | CampusNet messages and calendar | Darkened CampusNet's generated My Messages cards, filters, metadata, hover/focus states, and the legacy calendar's menu, empty state, and appointment-editor surfaces. Message subjects and calendar links use the active theme's soft accent rather than assuming DTU red. | On My Messages, verify read/unread cards, filters, subject links, hover/focus, pagination, and priority rails in dark mode; then turn Dark Mode off. On My Calendar, switch calendar views and open the non-submitting Add new appointment form; verify its menu, empty state, fields, and editor panel are dark and readable, then close it without saving. |
| 2026-07-14 | CampusNet front page | Completed an interaction-led dark-mode pass over the front-page feed, header search, profile/favorites menus, course and toolbox drawers, and expanded course groups. Fixed a state-specific contrast regression where dark-mode feed links reverted from the readable user-selected soft accent to a deep accent on hover; they now retain the chosen soft accent and underline while hovered. | On the CampusNet front page in dark mode, hover a message/file/deadline link and confirm it keeps the active theme's soft accent with an underline, then restores its normal appearance. Focus/hover the search controls, open/close favorites and profile menus, open/close My courses and Toolbox, and expand/collapse a course group. Turn Dark Mode off and confirm the native page colors return. |
| 2026-07-14 | DTU Course Base | Completed an interaction-led contrast pass over `kurser.dtu.dk` search, result, volume-picker, dropdown, and course-detail surfaces. Search checkboxes now use browser-native dark controls with the selected accent, empty-basket icons remain visible on dark result rows and hover, and extension-owned course-evaluation links/scores use the light accent intended for dark backgrounds instead of the low-contrast deep accent. | Pick the current course volume, inspect the search form in Firefox and Chrome, open and hover a multiselect, run a course search, and hover the empty-basket icons. Open a course with evaluation data and verify its score markers and `View full evaluation` link are readable. Turn Dark Mode off and confirm native controls, icons, and course colors return. |
| 2026-07-14 | DTU Evaluation legend | Corrected the comparison legend surface boundary after Firefox exposed the full fixed `.comparison__aside` as a viewport-covering dark-2 sheet. The structural aside now remains transparent while the actual legend card stays dark 2 and readable. | Open a course comparison result in both Firefox and Chrome. Verify the evaluation remains visible behind the fixed legend structure before, during, and after opening Legend, while the legend card itself remains dark 2. |
| 2026-07-14 | DTU Evaluation results | Completed a dark-mode pass over the comparison and bar-visualization result views, and fixed both manifests so the existing evaluering host-shell runtime is actually loaded on that host. The comparison tabs, sticky action footer, open legend drawer, and collapsible table of contents now use intentional dark surfaces with readable hover/focus states. Canvas-rendered charts receive a reversible dark-mode-only transform so their hard-coded black labels and white bands remain legible without changing native light mode. | Open a comparison result and a bar visualization on `evaluering.dtu.dk`. Hover and switch result tabs, open/close Legend, hover the table of contents, collapse/reopen it, and verify chart labels remain readable. Turn Dark Mode off and confirm the native light charts and panels return. Confirm both packaged manifests include `darkmode.host-shells.js` in the evaluering content-script entry. |
| 2026-07-14 | DTU Learn email composer | Darkened the Brightspace HTML editor across its open shadow root: the toolbar/editor chrome now uses dark 2, the TinyMCE writing canvas and iframe backing use dark 1, and borders/text remain readable instead of leaving white bands around the message body. | From Classlist, open a participant's compose-email window without sending. Verify the Body editor toolbar and bottom status/resize strip are dark 2, the editable message canvas is dark 1, controls/text remain legible, and Dark Mode off restores the native white editor. |
| 2026-07-14 | DTU Learn email | Removed Brightspace's native white gradient, inset highlight, and text shadow from selected-recipient chips. The multiselect field now has a solid dark input surface and each recipient remains readable on a solid dark chip after dynamic repainting. | Compose an email and add a recipient. Verify the recipient chip is solid dark with readable light text and remove icon, including after adding/removing recipients. |
| 2026-07-14 | DTU Learn Classlist | Made the complete legacy search surface dark 1: the collapsed Search/Show Search Options bar stays dark 1 on hover, and the expanded panel's outer tables, fieldsets, nested layout tables, hover rows, and search-header strip no longer expose dark-2 or generic hover-grey patches. Added an explicit `#z_g` fallback for Firefox that does not depend on `:has()`. Inputs and selects retain their raised control surfaces. | On Classlist with Dark Mode on, hover across the collapsed search bar, expand Search Options, and hover the header strip plus each filter row. Verify the complete panel background remains dark 1 while inputs/selects remain distinct. |
| 2026-07-14 | DTU Learn Calendar | Reclassified the Tasks widget's outer placeholder shell and title row as dark 1 while preserving the task datalist and editable controls as their own raised surfaces. | On Calendar with Dark Mode on, verify the background behind `Tasks` is dark 1 and the menu/task controls remain readable. |
| 2026-07-14 | DTU Learn Announcements | Split the legacy search UI's surface roles: the collapsed search-header wrapper is now dark 1, while the expanded Search Options form remains a dark-2 filter panel. | On Announcements with Dark Mode on, verify the collapsed Search/Show Search Options bar merges with the dark-1 page. Expand Search Options and verify the form below remains dark 2. |
| 2026-07-14 | DTU Learn legacy tools | Added the legacy-page styling path for the actual `d2l-floating-buttons` host. The host and its shadow wrappers now use dark 1 on Dropbox, Classlist, Groups, and News pages, where the normal dark-engine shadow pass is intentionally disabled. | On a legacy Learn form with Submit/Cancel actions, verify the full floating button strip is dark 1 after load and rerender; turn Dark Mode off and verify its native styling returns. |
| 2026-07-14 | DTU Learn Assignments | Reclassified the floating action rail as dark 1 (`#1a1a1a`). Added a `d2l-floating-buttons` shadow-root style so its internal container and inner container cannot be repainted light by Brightspace. | Open an Assignments page with Dark Mode on. Verify the whole floating action rail is dark 1 after load and rerender. |
| 2026-07-12 | DTU Learn dialogs | Made `.d2l-dialog-width` dialog bodies—including their form and structural `#d_content*` wrappers—dark 2 rather than dark 1. | Open a Learn dialog such as `Add a File` with Dark Mode on. Verify its complete body is one dark-2 surface, including after dynamic dialog content loads. |
| 2026-07-12 | DTU Learn navigation | Unified breadcrumb hosts, links, icons, and their internal shadow navigation list on dark 1. Breadcrumbs are structural navigation rather than dark-2 raised panels. | With Dark Mode on, inspect breadcrumbs on Learn pages. Verify the text and icons sit on one continuous dark-1 surface with no dark-2 patches. |
| 2026-07-12 | DTU Learn Assignments | Reclassified the floating action rail (`.d2l-floating-buttons-container`) as a dark-2 raised surface in both static and dynamic styling. | Open an Assignments page with Dark Mode on and verify the floating action rail and its inner container are dark 2 after load and rerender. |
| 2026-07-12 | DTU Learn Announcements | Made the legacy `#z_d_searchOptions` form a single dark-2 filter panel. Its nested layout tables now inherit that surface, and their generic row hover is neutralized instead of exposing a mismatched darker layer. | In Announcements with Dark Mode on, open Search Options. Move across the form and verify the entire filter panel remains one dark-2 surface, without dark-1 patches or partial table-row hover fills. |
| 2026-07-12 | DTU Learn Content/Lessons | Made the `tier2:file-document` content glyph background dark 2 in both its shadow stylesheet and the mutation-time icon styler, preventing a rerender from restoring its dark-1 patch. | Open any course Content page with Dark Mode on. Verify each document glyph sits on the same dark-2 surface as its topic card, including after expanding/collapsing sections. |
| 2026-07-12 | DTU Learn Content/Lessons | Unified the nested Content/Lessons item hierarchy by removing a competing `d2l-list-item-nav` shadow override that made section headers dark 1 at rest and dark 2 on hover, while their expanded topics were already dark 2. Sections and topics now share dark 2 as their contained surface, with dark 3 only for hover/focus. | In any course Content page with Dark Mode on, expand a section containing topics. Verify headers and topic rows use one stable dark-2 surface; hovering either raises only that row to dark 3, without a dark-1-to-dark-2 jump. |
| 2026-07-12 | DTU Learn Content/Lessons | Fixed the `tier1:search` and Course handbook `tier1:arrow-*-small` icon buttons' dark-mode hover/focus states inside their shadow roots, while preserving the shared baseline styling for unrelated controls such as Content Browser Actions. Their glyphs stay light while their interactive surfaces use the shared dark hover color instead of white. | Open a course Content page with Dark Mode on, hover and keyboard-focus the navigation Search and Course handbook controls. Verify both glyphs stay visible on a dark hover surface before and after expanding the handbook; verify the Content Browser Actions chevron retains its normal dark button and glyph. Turn Dark Mode off and verify native controls are unchanged. |
| 2026-07-12 | DTU Learn homepage | Applied the tested course-card Content shortcut geometry (`42px` square at `translate(175px, 60px)`) and removed the stale dark-engine rule that could override the shortcut module's placement. | Reload the Learn homepage, hover a course card, and verify the Content action has the same natural placement and size as the pin action. |
| 2026-07-12 | DTU Learn homepage | Repositioned the course-card Content shortcut directly beneath the native pin action and matched its width. Removed the experimental Deadlines timeline-rail redesign, its feature flag, and its Settings option so the original widget is always used. | Reload the Learn homepage, hover a course card, and verify the Content button is aligned directly beneath the pin button without clipping. Open Settings and confirm the experimental `Deadlines Timeline` option is gone; enable `Deadlines Widget` and verify the original tiled widget renders. |
| 2026-07-12 | DTU Learn Discussions | Extended semantic data-grid darkening to newer Brightspace MVC grids: dark-1 table headers, dark-2 topic rows, dark-3 whole-row hover, and consistent light text/borders. Added component-specific shadow styling for `d2l-button-subtle` so asynchronous Collapse/Expand controls keep dark surfaces and neutral readable labels instead of flashing white or link blue after rerender. | Open Discussions and wait through 2/5/12-second checkpoints. Verify topic grids remain stable, hovering a topic raises its entire row, and Collapse/Expand All Forums stays readable after the async update. Re-expand and wait 12 seconds for the complete Brightspace rerender. With Dark Mode OFF, verify normal and hover screenshots remain identical to the native/light page. |
| 2026-07-12 | DTU Learn Content/Lessons | Extended dark-mode shadow processing into same-origin Learn iframes so nested Smart Curriculum components such as `d2l-lessons-toc` and `d2l-lessons-restricted-text` receive their existing component-specific dark styles. Replaced the obsolete light selected-item workaround with dark-2 selected/control surfaces and dark-3 hover while preserving native section and selection-border colors. Dark Mode OFF does not inject the iframe shadow styles. | Open a course Content/Lessons page in dark mode and verify collapsed, selected, hovered, and expanded TOC items—including child units—use readable light text. Confirm the selected item remains identifiable through Brightspace's native colored borders. Repeat with Dark Mode OFF and verify native text/colors remain unchanged and no `dark-mode-shadow-styles-*` styles appear in the nested roots. |
| 2026-07-12 | DTU Learn Assignments | Restored the legacy Assignments data-grid hierarchy in dark mode: table headers/category bands use dark 1, assignment rows use dark 2, nested attachment layout tables stay transparent, and row hover uses the shared hover surface. This removes the native white cells/light-text collision and isolated black attachment blocks without changing Dark Mode OFF. | Compare the same course Assignments page with Dark Mode ON and OFF. In dark mode, verify every assignment/status cell is readable, attachment lists inherit the row surface, and hovering a row highlights its full width. With Dark Mode OFF, verify the page remains pixel-identical to the native/light rendering. |
| 2026-07-03 | Evaluation nag modal | The rare `#nagModal` popup (unanswered course evaluations) blacked out the whole screen because the generic `.modal` selector inlined an opaque dark-1 background onto its full-screen backdrop. The engine selector now excludes `#nagModal`, and the stylesheet gives it the native-style translucent scrim (`rgba(0,0,0,0.65)`) with a dark-2 content card and readable links/close button. | Trigger or simulate the evaluation nag popup in dark mode: the page must stay visible behind a dimmed scrim, with the evaluation links in a dark panel. |
| 2026-07-03 | Legacy Assignments table | The course Assignments (dropbox folders list) page kept D2L's white `tbody`/header-cell/`td` backgrounds because the blanket table rule intentionally sets no background. The legacy LMS styler now forces the `d2l-table` structure (`table/thead/tbody/tr/th/td`, `th.d_hch`, `td.d_gd`) transparent on legacy heavy pages so it inherits dark 1, matching the June legacy-tables convention. | Open Assignments in a course: the table (header row, No Category band, assignment rows) must be dark 1 with readable text. Sanity-check the classlist page for table regressions. |
| 2026-07-03 | Dark engine reliability + Lessons TOC | Fixed a `getBusUiApi` ReferenceError (shipped since 6.2) that aborted every mutation-driven dark pass after the first root, which was the main cause of finicky/late darkening site-wide. Also: the shadow-root safety sweep now runs in iframes (Lessons/Content SPAs live there), overflowing mutation batches escalate to a full sweep instead of silently dropping subtrees, `waitForCustomElements` no longer throws in sandboxed frames, and per-root failures can no longer strand the remaining batch. Redesigned Lessons TOC (`d2l-list-item-nav`): the selected/hovered item now gets the dark panel fill instead of a white highlight, and the panel overlay / new-content toast are darkened. | Open a course Lessons page: the left TOC must be fully dark immediately, including the currently selected item and on hover. Check the browser console for extension errors (should be none). |
| 2026-07-03 | Dark engine timing | Fixed finicky/late darkening on DTU Learn course, lessons, and calendar pages. Root causes: the 15s shadow-root safety sweep was never registered (its gate ran before the dark engine loaded), and D2L web components that upgrade after page load attach shadow roots invisibly to observers while per-element retries had already expired. The engine now re-sweeps (debounced) the moment a seen `d2l-*` tag is defined via `customElements.whenDefined`, sweeps once immediately at DOMContentLoaded, and the safety-net interval evaluates its gate per tick. On a throttled cold load, shadow-style coverage previously stalled at ~45% indefinitely; it now reaches full coverage within ~0.5s of components loading. | Cold-reload a course page with devtools network throttling (Slow 3G, cache disabled) and verify all Brightspace components darken within about a second of appearing instead of staying light. Normal loads should look unchanged. |
| 2026-07-03 | Textbook links | Merged the DTU Learn Book Finder and the kurser.dtu.dk Textbook Links into one shared `darkmode.textbooks.js` module behind a single `Textbook Links` toggle (existing kurser setting is reused; a stored disabled Book Finder setting migrates once to the unified flag). Behavior on both hosts is unchanged. | Verify link bars still appear on a kurser course page with literature and on a Learn course page containing an ISBN, and that the single Settings toggle removes/restores both. |
| 2026-07-03 | Participant intelligence | Participant Intelligence (course composition, shared course history, Retention Radar) is now opt-in: all four feature flags default to off for new installs. Users who previously enabled them keep their stored setting. No participant data is collected or stored until the feature is enabled in Settings. | On a fresh profile, open a CampusNet participant page and verify no demographics/shared-history/retention UI appears and no `dtuParticipantIntel` key is written. Enable Participant Intelligence in Settings and verify the features come back. |
| 2026-07-03 | Participant intelligence | Removed the Semester Twins feature entirely: the DTU Learn/CampusNet twin widgets, the `darkmode.semester-twins.js` UI shell, the now-unused `darkmode.participant-intel-scoring.js` scoring module, twin preferences storage, and all bridge wiring. Stored twin prefs/flags are cleaned up once on load. Shared course history, demographics, and Retention Radar are unaffected. | Reload CampusNet frontpage and a participant page: no Semester Twins widget appears anywhere, while shared-history badges, demographics, and Retention Radar still work. Settings modal no longer lists a Semester Twins toggle. |
| 2026-07-03 | Performance / manifests | Split the content-script blocks for DTU Learn, CampusNet, Study Planner, and kurser so only the core dark-mode scripts (`config.js`, `darkmode.js`, `darkmode.dark-engine.js`, plus `darkmode.learn-accent-shell.js` and `darkmode.smart-room-linker.js` where iframe styling needs them) run in every frame. All other feature scripts now load with `all_frames: false`, so iframes no longer parse ~1 MB of top-window-only feature code. No feature behavior changes: every moved script was already gated to the top window at runtime. | Reload DTU Learn and open a course Content/Lessons page: iframe content must still darken. Verify homepage widgets (bus/deadlines/library), CampusNet grades/participants, Study Planner, and kurser panels still appear. |
| 2026-05-10 | Release version | Bumped the public browser-extension manifests to `6.2.1` so web stores can accept a new submission after the listed `6.2` release. | Rebuild Firefox and Chrome packages and upload the generated zip files from `dist/`, not the unpacked manifest file. |
| 2026-05-10 | DTU Learn dark nav links | Fixed Chrome dark-mode nav links where native anchors such as `Calendar`, `Study Rules`, and `Atomic Search` could fall back to browser-blue `a:link` styling inside Brightspace navigation. The dark stylesheet and shadow-root dark engine now explicitly cover `:link`, `:visited`, `:hover`, and `:active` states for Learn main-nav anchors. | In Chrome with dark mode enabled, reload DTU Learn and verify `Calendar`, `Study Rules`, and `Atomic Search` render with the same neutral nav text color as the dropdown buttons. |
| 2026-05-10 | Cookie/header hygiene | Removed the legacy `.dtu.dk` `dtuDarkMode` cookie mirror for dark-mode preference and added best-effort cleanup of the old cookie. Dark-mode state now relies on site `localStorage` plus extension storage, reducing any extension contribution to oversized DTU cookie request headers. | Visit a DTU domain with the extension enabled, then inspect cookies for `.dtu.dk` and verify `dtuDarkMode` is no longer recreated. Dark-mode toggling should still persist across supported DTU sites. |
| 2026-05-10 | Study Planner exam cluster | Temporarily disabled the `Exam Schedule & Gaps` widget in public builds while exam-date validation is revisited. The runtime now has a public kill-switch so existing users with the old setting enabled will not see the widget, and the Settings toggle has been removed to avoid accidental re-enabling. | Reload `studieplan.dtu.dk` and verify no `Exam Schedule & Gaps` panel or explanatory placeholder is injected. |
| 2026-05-10 | DTU Library panel data | Restored the Library panel data bridge after the refactor by making `darkmode.js` Library deps late-safe and replacing old monolith globals inside the remaining Library data/chart runtime in `darkmode.bus.js`. Library events/news now fetch through the background broker again, stale empty feed caches are force-refreshed once, and Library panel data/render calls are guarded so a single failed dependency cannot collapse the modal back to an empty fallback. | Reload DTU Learn, open `Library`, and verify Upcoming Events and Library News populate. If occupancy trends are configured through `config.local.js`/`LIVE_LIBRARY_TRENDS_URL`, verify the visitor counts and crowding chart render; otherwise confirm the panel clearly says shared occupancy is unavailable while feeds still work. |
| 2026-05-10 | kurser.dtu.dk insight widgets | Reduced the visual scale of the injected `Grade Statistics` and `Course Evaluation` panels so they sit closer to the native course-page proportions. Shared panel padding, surface padding, heading sizes, summary numbers, histogram height, satisfaction rows, and workload gauge sizing were tightened further without changing the data/render logic. | Open a `kurser.dtu.dk/course/...` page with Grade Statistics and Course Evaluation enabled, then verify both panels feel compact relative to the course page while the chart bars, satisfaction scores, and workload gauge remain readable in dark and light mode. |
| 2026-05-10 | Study Planner exam cluster | Fixed the refactored `studieplan.dtu.dk` exam-cluster runtime so it no longer references old monolith globals (`IS_TOP_WINDOW`, `darkModeEnabled`, and direct feature-flag symbols). The widget now uses the runtime dependency bridge, retries after the render UI is ready, and bootstraps on DOM/load/pageshow so it can mount after Study Planner finishes rendering. | Reload `studieplan.dtu.dk` with `Study Planner Exam Cluster` enabled and verify the `Exam Schedule & Gaps` widget appears. Toggle the feature off/on in Settings if available and verify the panel removes/reappears cleanly. |
| 2026-05-10 | DTU Library modal opener | Made the Library modal opener independent of nav ownership by adding a delegated capture click handler in the Library module itself. The panel now has fallback styling/content and no longer refuses to render feed fallback states when optional runtime dependencies are missing. | Reload DTU Learn, click the `Library` nav item, and verify the modal opens even if crowding/news/events data is unavailable. |
| 2026-05-10 | DTU Learn content shortcut | Restored Content Shortcut injection on the current Brightspace course cards by scanning both legacy `d2l-enrollment-card` and newer `d2l-my-courses-enrollment-card` hosts. The bootstrap now checks for injected buttons through shadow DOM and retries longer while the homepage cards render. | Reload the DTU Learn homepage with `Content Shortcut` enabled, hover a course card, and verify the content shortcut button appears and links to the course Lessons page. |
| 2026-05-10 | DTU Library nav toggle | Routed the Settings `Library` toggle through the new Learn navigation owner instead of the legacy library-module nav inserter. Disabling removes the injected `Library` nav item, and re-enabling now reinserts the same native Learn nav button that opens the Library panel. | Toggle `Library` off and on in Settings, then verify the `Library` nav item disappears and returns without a page reload. Click it and confirm the Library modal opens. |
| 2026-05-10 | DTU Learn Deadlines widget | Fixed Deadlines widget startup on the DTU Learn default home route (`/d2l/lp/ouHome/defaultHome.d2l`) and added a retry bootstrap so the widget waits for Brightspace homepage columns before giving up. | Enable `Deadlines Widget`, reload the extension and DTU Learn homepage, and verify the widget appears after the page finishes rendering. Confirm it still disappears when the setting is turned off. |
| 2026-05-10 | DTU Library panel | Hardened the refactored Library panel opener so it no longer silently fails when the trend/crowding dependency surface is late or partially initialized. The bus module now merges the Library runtime dependencies into any existing deps object, and the panel renders a fallback trend section instead of aborting before the modal reaches the DOM. | Reload DTU Learn, click the `Library` nav item, and verify the DTU Library modal opens. Confirm quick links and feeds render, and that the crowding/trend section either loads normally or shows a clear unavailable state instead of preventing the modal from opening. |
| 2026-05-10 | DTU Learn popovers | Added dark-mode shadow-root overrides for Brightspace popover background/foreground/border variables and the empty dropdown header shell. This prevents dropdown/popover internals from falling back to the component's white default background in dark mode. | In dark mode, open Brightspace dropdowns/popovers that render `.dropdown-header.dropdown-no-header` and verify the header/popover surface stays dark instead of flashing white. |
| 2026-05-10 | DTU Learn navigation | Moved ownership of the `Library` nav button into the same Learn navigation bootstrap that owns `Settings`, while keeping the existing library panel UI in `darkmode.library.js`. The settings label `Smart Room Links` is now shown as `Room links`. | Reload DTU Learn and verify both `Settings` and `Library` appear in the main navigation row. Click `Library` and confirm the existing DTU Library panel opens. Open Settings and verify the smart-room toggle is labelled `Room links`. |
| 2026-05-10 | DTU Learn Course Search toggle | Fixed the `Course Search` setting so the saved disabled state is enforced on page reload, not only after toggling it live. The Atomic Search nav item is now found through shadow-DOM traversal, and the homepage course-search widget is hidden independently of whether the Deadlines widget is enabled or mounted yet. | In Settings, turn `Course Search` off, reload DTU Learn, and verify both `Atomic Search` in the nav row and the homepage search widget stay hidden. Turn it on again and reload to confirm both return. |
| 2026-05-10 | DTU Learn Library nav | Restored the `Library` entry as a native Learn navigation button beside the refactored `Settings` entry. The library module now uses the same shadow-DOM-aware main-navigation discovery, visibility forcing, and Brightspace button structure as Settings, so it is reinserted into the footer nav row after late Brightspace rerenders. | Reload DTU Learn and verify `Library` appears in the main navigation row near `Atomic Search`/`Settings`. Click it and confirm the DTU Library panel opens, closes, and still refreshes links/news/events/crowding data where configured. |
| 2026-05-09 | DTU Learn Admin Tools | Restored the pre-refactor `Settings` entry as a native Learn navigation button in the same footer row as `Help`, `Atomic Search`, and the other Brightspace nav items. The injected button uses Brightspace's own `d2l-navigation-s-group d2l-dropdown-opener` structure and opens the standalone settings modal, while the native course-search/Atomic Search entry now stays enabled by default and the dark stylesheet is re-anchored after DOM bootstrap. | Reload DTU Learn and verify `Settings` appears in the main navigation row after `Atomic Search` when it is visible, or before `More`/after `Help` as fallback. Click it and confirm the standalone settings modal opens and can control extension actions. |
| 2026-05-09 | Study Planner exam helper | Hardened exam-date matching so only official course-code matches and exact full slot matches such as `F1-A` auto-place dates. Base placements such as `F1`/`E2`, period-only matches, and other fuzzy inferred matches now require a user choice instead of auto-selecting the earliest broad exam-period date. | On `studieplan.dtu.dk`, verify a course with a base placement opens the exam-choice prompt with the matching A/B dates. Confirm exact placements such as `F1-A` and official course-code rows still map directly, while period-only/fuzzy matches do not auto-populate the timeline. |
| 2026-04-21 | Dark engine module split | Extracted the shared dark-mode/shadow-root runtime from `darkmode.js` into a new public `darkmode.dark-engine.js` content script. Shadow-root style injection, dynamic inline overrides, iframe/html-block rescans, and `runDarkModeChecks` now live in a dedicated module, while `darkmode.js` keeps the bootstrap/unified-observer bridge and shared host-level helpers. The manifests, whitelist, build scripts, and maintainer docs now ship/document the new file across the supported DTU hosts. | Reload DTU Learn, CampusNet, and one non-Learn DTU host in dark mode, then verify the shared darkening still applies, Brightspace shadow-DOM surfaces still darken correctly, and both browser zips package `darkmode.dark-engine.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Study Planner exam runtime split | Extracted the remaining Study Planner exam parsing/timeline runtime into a new public `darkmode.studyplan-runtime.js` content script. Course extraction, exam-calendar matching, timeline override persistence, and refresh scheduling/state now live in a dedicated runtime module, while `darkmode.studyplan-exams.js` stays focused on rendering/editor UI and `darkmode.js` keeps only thin bridge wrappers. | Reload `studieplan.dtu.dk` and verify the exam timeline still appears, ambiguous exam choices still open in the editor, manual timeline edits still persist, and the cluster still refreshes when the page rerenders. Rebuild both browser zips and confirm `darkmode.studyplan-runtime.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | DTU Learn accent shell module split | Extracted the DTU Learn accent-band and legacy LMS shell styling cluster into a new public `darkmode.learn-accent-shell.js` content script. Accent-band forcing, counter/accent badge polishing, mobile-nav shell styling, and legacy LMS cleanup now live in a dedicated module, while `darkmode.js` keeps only thin bridge wrappers and runtime call sites. The manifests, whitelist, build scripts, and maintainer docs now ship/document the new file for the DTU Learn host bucket. | Reload DTU Learn in both light and dark mode, then verify the top accent band still stays branded, counters/badges keep the accent treatment, mobile-nav shells still darken correctly, legacy LMS tool pages still lose their forced bright backgrounds, and both browser zips package `darkmode.learn-accent-shell.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Host shell module split | Extracted the CampusNet/evaluering host-shell styling cluster into a new public `darkmode.host-shells.js` content script. CampusNet shell restyling, evaluering chart-shell fixes, and the existing student-portal shell styling boundary now live in a dedicated module, while `darkmode.js` keeps only thin bridge wrappers and the runtime call sites. The manifests, whitelist, build scripts, and maintainer docs now ship/document the new file for the relevant host buckets. | Reload CampusNet and `evaluering.dtu.dk`, then verify CampusNet header/widget/breadcrumb shell styling still applies in dark and light mode, evaluering chart wrappers still stay transparent and render correctly, and both browser zips package `darkmode.host-shells.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Study Planner shell expansion | Moved the remaining `typebox` color-preservation helpers and `kurser.dtu.dk` light-mode accent-link styling into `darkmode.studyplanner-shell.js`. The shared Study Planner / kurser shell module now owns those host-shell styling concerns as well, while `darkmode.js` keeps only thin bridge wrappers and call sites. | Reload `studieplan.dtu.dk` and `kurser.dtu.dk`, then verify `.typebox` elements keep their inline background colors in dark mode, `kurser.dtu.dk` info-box/search/evaluation links still get the accent-link treatment in light mode, and the existing tab/top-bar/basket shell styling still behaves as before. |
| 2026-04-21 | Study Planner shell module split | Extracted the shared `studieplan.dtu.dk` / `kurser.dtu.dk` shell-accent logic into a new public `darkmode.studyplanner-shell.js` content script. Accent-bar/tab styling, Study Planner top-link polish, and basket-action repositioning now live in a dedicated module, while `darkmode.js` keeps only a thin bridge wrapper. The manifests, whitelist, build scripts, and maintainer docs now ship/document the new file for the Study Planner and kurser host buckets. | Reload `studieplan.dtu.dk` and `kurser.dtu.dk`, then verify the accent tabs still render, the Study Planner top-right links stay readable/accented, the basket action row still moves into the basket title area, and both browser zips package `darkmode.studyplanner-shell.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Smart room linker module split | Extracted the shared MazeMap smart-room linker into a new public `darkmode.smart-room-linker.js` content script. Site-wide room-text detection, MazeMap link creation, tooltip/loading state, and shadow/html-block rescans now live in a dedicated module, while `darkmode.js` keeps only thin bridge wrappers and shared runtime dependencies. The manifests, whitelist, build scripts, and maintainer docs now ship/document the new file across DTU Learn, CampusNet, kurser, and the other supported host buckets. | Reload DTU Learn, CampusNet, and a `kurser.dtu.dk` course page, then verify room mentions still become MazeMap links, lazy resolution still opens exact locations on click, and the packaged Firefox/Chrome builds include `darkmode.smart-room-linker.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Settings module split | Extracted the standalone DTU Learn settings shell into a new public `darkmode.settings.js` content script. The settings modal, paused-URL manager, disclaimer/footer actions, and immediate settings-application UI now live in a dedicated module, while `darkmode.js` keeps only the runtime bridge and shared feature/state helpers. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Reload DTU Learn and verify the `Settings` nav item and admin gear still open the standalone modal, feature toggles still apply immediately, `Paused URLs...` still opens/saves/removes rules correctly, and both browser zips package `darkmode.settings.js` without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Settings cleanup | Removed the last dead Brightspace-dropdown settings renderer from `darkmode.js`. The standalone Settings modal remains the only live settings surface, so the repo no longer carries a duplicate legacy Admin Tools panel implementation with stale debug/dropdown dependencies. | Reload the DTU Learn homepage and confirm the `Settings` nav item and admin-gear entry still open the standalone modal, feature toggles still sync/apply immediately, and no old dropdown-based settings surface appears. Rebuild both browser zips and confirm public modules still package without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | DTU Learn shell module split | Extracted the remaining DTU Learn shell extras into a new public `darkmode.learn-shell.js` content script. Mojangles header branding, the first-run settings onboarding hint, and the dev-only context-capture helper now live in a dedicated module, while `darkmode.js` keeps only the runtime bridge and bootstrap calls. This also removes the old duplicate context-capture feature-flag split so the dev-only helper follows the single `ENABLE_CONTEXT_CAPTURE_DEV_TOOL` boundary. | Reload the DTU Learn homepage and a Learn lesson page, then verify the Mojangles overlay still appears when enabled, the first-run settings hint still targets the Settings nav item/admin gear, and the build continues to keep the context-capture helper disabled by default. Rebuild both browser zips and confirm `darkmode.learn-shell.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | DTU Learn nav shell module split | Extracted the remaining DTU Learn nav shell into a new public `darkmode.learn-nav.js` content script. `Student Resources` quick-link injection/reordering, homepage `Help` removal, and the `Settings` nav item insertion now live in a dedicated module, while `darkmode.js` keeps the actual settings modal and the thin feature/runtime bridge. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Reload the DTU Learn homepage and verify `Student Resources` still gets `Panopto` and `CampusNet`, the menu reorder still applies, `Help` is removed on the homepage, and the `Settings` nav item still opens/closes the settings modal correctly. Rebuild both browser zips and confirm `darkmode.learn-nav.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | DTU Learn book finder module split | Extracted the DTU Learn course-page book-finder shell into a new public `darkmode.book-finder.js` content script. ISBN/title detection, title heuristics, and the injected library/bookshop/marketplace link bars now live in a dedicated module, while `darkmode.js` keeps the shared ISBN helpers, `isNotesOnlyLiterature()` heuristic, and the feature/runtime bridge. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Open a DTU Learn course page with textbook references in plain text, quoted titles, or ISBN form and verify the book-finder bars still inject correctly in dark mode and light mode. Rebuild both browser zips and confirm `darkmode.book-finder.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Kurser textbook linker module split | Extracted the `kurser.dtu.dk` textbook-linker shell into a new public `darkmode.kurser-textbooks.js` content script. Literature-section detection, citation parsing, FindIt/Google Books link injection, and rollback/rehydration of rewritten literature sections now live in a dedicated module, while `darkmode.js` keeps the shared ISBN helpers, course-page detection, and DTU Learn book-finder logic. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Open a `kurser.dtu.dk/course/...` page with literature listed in both bar-section and plain-text formats and verify the textbook links still inject, duplicate lines stay deduped, `Free PDF` upgrades still appear when available, and disabling the feature removes the injected UI cleanly. Rebuild both browser zips and confirm `darkmode.kurser-textbooks.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Kurser course evaluation module split | Extracted the `kurser.dtu.dk` course-evaluation shell into a new public `darkmode.kurser-course-eval.js` content script. Evaluation link discovery, retry timing, remote fetch/render flow, and the evaluation panel UI now live in a dedicated module, while `darkmode.js` keeps the shared kurser helpers and literature parser. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Open a `kurser.dtu.dk/course/...` page and verify the `Course Evaluation` panel still appears, retries when data is late/unavailable, and links through to the full evaluation view. Rebuild both browser zips and confirm `darkmode.kurser-course-eval.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Kurser widgets module split | Extracted the `kurser.dtu.dk` course-widget shell into a new public `darkmode.kurser-widgets.js` content script. MyLine curriculum badges, grade statistics, room finder rendering, and schedule annotation now live in a dedicated module, while `darkmode.js` keeps the shared kurser helpers plus the textbook-linker and course-evaluation logic. The manifests, whitelist, build scripts, and codebase guide now ship/document the new file. | Open a `kurser.dtu.dk/course/...` page and verify the MyLine badge, grade stats panel, room finder row, and schedule annotation still render/toggle correctly. Rebuild both browser zips and confirm `darkmode.kurser-widgets.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Bus module split | Finished the DTU Learn bus extraction by moving the remaining bus runtime into `darkmode.bus.js`. Bus config/state, quota handling, shared-cache/lease logic, fetch orchestration, smart polling, the top-nav departures widget, first-run setup prompt, config modal, and admin-tools bus toggle now live together in one dedicated public module, while `darkmode.js` keeps only a thin bridge plus the generic DTU Learn page-detection helpers. | Reload the DTU Learn homepage and verify the nav bus widget still renders, the first-run prompt still opens the config flow, the bus config modal still saves campus/line/direction choices, the admin-tools `Bus Departures` toggle still enables/disables the feature, and refresh/polling still resumes when the tab becomes visible again. Rebuild both browser zips and confirm `darkmode.bus.js` is packaged without `data/`, `infra/`, or room-data files. |
| 2026-04-21 | Semester Twins compact layout | Redesigned the DTU Learn Semester Twins rows into a denser list-style layout instead of tall profile cards. Names and `Twin` status now share the top line, overlap data is presented inline, the score ring/right rail is smaller, and the shared-course drawer is tighter, so the widget scans faster and uses much less height without changing the ranking/content logic. | Reload the DTU Learn homepage and verify the Semester Twins widget feels visibly more compact and list-like, with shorter rows, inline overlap metadata, a smaller score treatment, and a tighter shared-course drawer while still keeping names, programs, and match strength readable. |
| 2026-04-21 | DTU Learn course search toggle | Renamed the settings description for `Course Search` to `Native course search on the dashboard` and extended the toggle so it also hides the top navigation `Atomic Search` menu item when disabled. The setting now controls both the homepage native search widget and the matching Learn navigation entry together. | Open the DTU Learn settings menu and confirm the `Course Search` description uses the new wording. Toggle it off and verify both the homepage `Search my courses` widget and the top-nav `Atomic Search` item disappear; toggle it back on and confirm both return. |
| 2026-04-21 | DTU Learn homepage deadlines placement | Adjusted the homepage `Deadlines` widget placement again so it mounts above `Study Announcements` in the main left widget column instead of following the right-side search/sidebar column. The placement logic now uses the actual `Study Announcements` widget container as its preferred anchor, while the `Search my courses` widget remains in the right column. | Reload the DTU Learn homepage and verify `Deadlines` appears above `Study Announcements` on the left, while `Search my courses` stays on the right. Confirm the overall homepage structure still looks native after the widget moves. |
| 2026-04-21 | DTU Learn homepage deadlines placement | Fixed the homepage `Deadlines` widget mount logic after Brightspace layout changes made `.homepage-col-3` point at the wrong column. The widget now prefers the actual Atomic search widget's parent column instead of blindly targeting `.homepage-col-3`, and the old forced `.homepage-col-3` width override is disabled so the homepage structure no longer gets stretched into the wrong composition. | Reload the DTU Learn homepage and verify `Deadlines` appears in the same sidebar column as the `Search my courses` widget instead of the main left content column. Confirm the overall homepage column widths look native again with no widened/stretched `homepage-col-3` layout. |
| 2026-04-21 | Content shortcut module split | Extracted the DTU Learn course-card `Content` shortcut feature into a new public `darkmode.content-shortcut.js` content script. The new module now owns course-card button insertion, per-course override storage sync, and the shortcut-link editor modal, while `darkmode.js` keeps only the shared deep-query helper plus thin compatibility wrappers used by settings/admin flows. The manifests, whitelist, and build scripts now ship the new file in both browser packages. | Reload the DTU Learn homepage, then verify course cards still show the `Content` button, Ctrl/Cmd+Shift-click still opens the per-course editor, the settings `Edit` action still opens the override manager, and saved overrides still survive reloads/storage sync. Rebuild both browser zips and confirm `darkmode.content-shortcut.js` is packaged. |
| 2026-04-21 | Participant intelligence core module split | Extracted the shared participant-intelligence storage/parser core into a new public `darkmode.participant-intel-core.js` content script. Browser-storage persistence, participant-course deduping, Semester Twins preferences, participant-page collection, and self-detection now live in a dedicated module, while `darkmode.js` keeps thin compatibility wrappers plus the remaining host/runtime orchestration. The manifests, whitelist, and build scripts now ship the new file in both browser packages, including the Learn-side Semester Twins flow. | Reload DTU Learn and CampusNet, then verify Semester Twins still loads its stored data/preferences, CampusNet participant pages still collect/update participant history, and existing retained data continues to render correctly. Rebuild both browser zips and confirm `darkmode.participant-intel-core.js` is packaged. |
| 2026-04-21 | Participant intelligence backfill module split | Extracted the CampusNet archive backfill/history-scanner subsystem into a new public `darkmode.participant-intel-backfill.js` content script. Archive-page parsing, participant-list fetch/backfill flow, progress state, and the `Course History Scanner` widget now live in a dedicated module, while the participant-intel storage core and Semester Twins scoring remain in `darkmode.js`. The manifests, whitelist, and build scripts now ship the new file in both browser packages. | Reload the CampusNet group archive and Semester Twins CampusNet controls, then verify the `Course History Scanner` still inserts, starts/stops a scan, updates progress text, and reuses already-scanned courses correctly. Rebuild both browser zips and confirm `darkmode.participant-intel-backfill.js` is packaged. |
| 2026-04-21 | Participant intelligence module split | Extracted the CampusNet participant-intelligence UI shell into a new public `darkmode.participant-intel-ui.js` content script. Course Composition, shared-history badges, profile history, and Retention Radar rendering now live in a dedicated module, while the participant scanner/storage/archive-backfill logic stays in `darkmode.js`. The manifests, whitelist, and build scripts now ship the new file in both browser packages. | Reload CampusNet participant pages and profile pages, then verify `Course Composition`, shared-history badges, profile history cards, and `Retention Radar` still render and refresh correctly. Rebuild both browser zips and confirm `darkmode.participant-intel-ui.js` is packaged. |
| 2026-04-21 | Semester Twins module split | Extracted the Semester Twins DTU Learn/CampusNet widget shell into a new public `darkmode.semester-twins.js` content script. Widget placement, rendering, theming, and CampusNet frontpage retry logic now live in a dedicated module, while the participant-intel data model and twin scoring stay in `darkmode.js`. The manifests, whitelist, and build scripts now ship the new file in both browser packages. | Reload DTU Learn and the CampusNet frontpage, then verify the Semester Twins widget still inserts in both places, the filter menu still updates the rendered results, and the CampusNet frontpage version still reappears after late DOM/widget mounts. Rebuild both browser zips and confirm `darkmode.semester-twins.js` is packaged. |
| 2026-04-21 | CampusNet GPA module split | Extracted the CampusNet Grades GPA feature into a new public `darkmode.campusnet-gpa.js` content script. Weighted GPA, ignored-grade toggles, ECTS progress, and the hypothetical-grade simulator now live in a dedicated module, with the manifests/build scripts updated so the new file ships in both browser packages. | Open the CampusNet Grades page and verify the weighted GPA row, `Hide/Back` ignored-grade controls, ECTS progress bar, and hypothetical GPA simulator still load and update correctly. Rebuild both browser zips and confirm `darkmode.campusnet-gpa.js` is included. |
| 2026-04-21 | Library module split | Extracted the DTU Learn library nav/panel shell into a new public `darkmode.library.js` content script, left the library chart/data logic in `darkmode.js`, and updated the manifests, whitelist, maintainer docs, and build scripts so the feature boundary is easier to understand and the new file is included in packaged Firefox/Chrome builds. | Reload the extension on the DTU Learn homepage and verify the `Library` nav item still inserts, opens, closes on outside click/Escape, refreshes the feed sections, and keeps the trend panel working. Rebuild both browser zips and confirm `darkmode.library.js` is packaged. |
| 2026-04-21 | Codebase guide and dead-code trim | Expanded `docs/CODEBASE.md` as the public maintainer/AI guide and removed a few genuinely unused helper functions flagged by the repo audit script. | Open `docs/CODEBASE.md` and confirm the runtime/file ownership notes match the current repo. Run `node scripts/audit-unused-functions.mjs darkmode.js` and verify the removed high-confidence dead functions no longer appear. |
| 2026-04-21 | Library shared-data boundary and repo code map | Removed the direct FindIt occupancy fallback from the public extension path so the library crowding UI is shared-endpoint-only, and added `docs/CODEBASE.md` to map the runtime/file ownership for maintainers and AI agents. Public source builds now show library links/news/events without live crowding unless `LIVE_LIBRARY_TRENDS_URL` is supplied through local config. | In a source build without a local library trends override, open the DTU Library panel and verify links/news/events still load while live occupancy shows as unavailable. Review `docs/CODEBASE.md` for the new file/runtime map before future refactors. |
| 2026-04-21 | DTU Library after-hours occupancy guard | Stopped the library crowding panel from presenting overnight source values as normal daytime occupancy. Outside the `08:00-22:00` chart window, the hero now switches to `After hours`, hides the filled percentage and live counts, removes the misleading current marker from the graph, and replaces the old `Based on 1 local sample from today.` wording with neutral shared-trend copy. | Open the DTU Library panel around `03:00` or another off-hours time. Verify the hero shows `After hours`, the `Now`/`Today`/`Free` values are hidden, the chart no longer pins a fake current marker to `08:00`, and the confidence line no longer mentions `local sample(s)`. |
| 2026-04-21 | DTU Learn collapsible panel header | Darkened Brightspace collapsible-panel headers inside shadow DOM when the component enters its `.scrolled` state. The `Upcoming events`-style header no longer falls back to a white background in dark mode, and its opener/icon surfaces stay transparent on the dark header. | On a DTU Learn page with a `d2l-collapsible-panel`, scroll until the host gets the `scrolled` state and verify the header background stays dark instead of turning white. Confirm the title button text and chevron icon remain readable in dark mode. |
| 2026-04-21 | DTU Learn deadlines widget | Deduplicated identical deadline rows in the homepage `Deadlines` widget when DTU publishes the same registration window under multiple semester headings. This prevents the June/July/August 3-week registration period from appearing as three visually identical cards when the widget is in compact view. | On the DTU Learn homepage, open the `Deadlines` widget while the course-deadline source contains repeated `Registration period: 1/5 2026 - 15/5 2026` entries across multiple summer headings. Verify only one card is shown for that shared registration window, while distinct supplementary/withdrawal deadlines still appear normally. |
| 2026-04-21 | Repository privacy boundary | Switched `.gitignore` to an explicit public-file whitelist, removed stale manifest references to the local room-data bundle, and updated the README to document the stricter public-vs-local split. This keeps infra, data, private notes, build outputs, and scratch files out of the public repo by default. | Verify `.gitignore` only re-allows the intended public files/directories, confirm `manifest.json` and `manifest_chrome.json` no longer reference `data/rooms_spring_2026.json`, and check the README build section for the new public/local boundary note. |
| 2026-03-26 | kurser.dtu.dk course insights sizing | Tightened the `Grade Statistics` and `Course Evaluation` widgets after the initial UI refresh made them read too large. The shell padding, summary metrics, histogram, satisfaction rows, and workload gauge now use a more compact rhythm while keeping the flatter layout. | Open a `kurser.dtu.dk/course/...` page in dark mode and light mode. Verify both widgets feel smaller and denser than the previous refresh, without losing readability or breaking histogram/question/workload alignment. |
| 2026-03-26 | DTU Learn bus line tile flattening | Removed the remaining rounded-card language from the bus line setup flow. Configured lines and `Add Bus Line` options now use hard-edged row/tile surfaces with sharper route badges, a stronger accent rail, and no hover lift, so the picker reads like flat transport controls instead of pill boxes. | Open the bus widget setup modal on the DTU Learn homepage in dark mode and light mode. Verify the configured-line list and `Add Bus Line` options no longer look rounded or pill-shaped, and confirm hover/click/back flows still work for all available lines. |
| 2026-03-26 | Semester Twins filter menu sizing | Reduced the footprint of the Semester Twins filter menu on both DTU Learn and CampusNet. The dropdown is now narrower with tighter padding, smaller controls, and less internal spacing, so it reads as a lighter utility menu instead of a secondary panel. | Open the Semester Twins filter menu in dark mode and light mode on both the homepage widget and the CampusNet version. Verify the menu is visibly smaller, still readable, and that the scope/show/study-line/course-history controls remain usable. |
| 2026-03-26 | DTU Library crowding panel flattening | Removed the remaining generic section-card shell from the library crowding block. The occupancy trend now sits directly on the panel surface instead of inside a rounded gray card, and the chart area no longer inherits that extra gray backdrop. | Open the DTU Library panel in dark mode and light mode. Verify the crowding/occupancy section no longer appears inside a separate rounded card, and confirm the chart background now reads as part of the main panel surface rather than a gray inset box. |
| 2026-03-26 | CampusNet Retention Radar UI | Moved `Retention Radar` out of the nearly invisible `Users` heading badge and into the participant-page insight area. The main experience now lives inside `Course Composition`, where it shows current users, net percentage change across the stored history window, peak seen count, and the latest move since the previous snapshot. A standalone fallback card is still used when `Course Composition` is disabled. | Open a CampusNet participant page (`Users` list) with Retention Radar enabled in dark mode and light mode. Verify the retention summary appears inside `Course Composition` when that widget is on, or as its own card when composition is off. Confirm current users, percentage change, peak count, and latest-move text update after revisiting the course on a later day. |
| 2026-03-26 | kurser.dtu.dk course insights UI | Restyled the `Grade Statistics` and `Course Evaluation` widgets into flatter course-insight dashboards. The summary metrics now rely on typography and spacing instead of nested cards, the grade distribution breathes more, and the evaluation satisfaction/workload sections use calmer surfaces with clearer dividers and stronger value hierarchy. | Open a `kurser.dtu.dk/course/...` page with both widgets enabled in dark mode and light mode. Verify the outer course-insight panels feel less boxed in, the metric summaries remain readable, the distribution bars still map correctly, and the course-evaluation footer link/workload marker/question scores all still align correctly. |
| 2026-03-26 | DTU Learn bus line picker UI | Restyled the `Add Bus Line` picker into a wider, flatter selection step. Route options now sit on calm surface tiles with stronger route-name hierarchy, route-color badges stay prominent, campus coverage is shown as clean secondary text instead of stacked pills, and the view uses more breathing room overall. | Open the bus widget setup/config modal on the DTU Learn homepage and enter `Add Bus Line` in both dark mode and light mode. Verify the picker feels like one open modal surface rather than a stack of bordered cards, and confirm hover, click, and back-navigation still work for all listed lines. |
| 2026-03-26 | DTU Library crowding chart UI | Flattened the remaining nested card shells inside the library crowding section. The occupancy metrics row and chart area now read as one continuous surface instead of two extra rounded containers sitting inside the trend panel. | Open the DTU Library panel in dark mode and light mode. Verify the crowding section no longer shows a separate rounded box around the top metrics row or another around the chart, while the chart, progress bar, legend, and event notes remain readable. |
| 2026-03-26 | CampusNet Semester Twins UI | Restyled Semester Twins on both the Learn widget and the CampusNet frontpage into a flatter, calmer match explorer. Match rows now use stronger typography, lighter surfaces, clearer score hierarchy, and a roomier expandable shared-course drawer, while the filter/scan panel uses larger controls and less heavy accent framing. | Open Semester Twins on both the homepage widget and CampusNet frontpage in dark mode and light mode. Verify the header no longer feels boxed in, the filter drawer remains readable, match cards expand/collapse cleanly, and shared-course lists are easier to scan without pill-heavy chips. |
| 2026-03-26 | Study Planner exam timeline | Refined the `Exam Schedule & Gaps` UI into a more intentional timeline layout. The widget now uses a calmer panel shell, a compact summary strip, stronger next-exam hierarchy, cleaner gap labeling, and a roomier modal editor with flatter row treatments and accent-aware controls. | On `studieplan.dtu.dk`, open `Exam Schedule & Gaps` in dark mode and light mode. Verify the top summary metrics, the promoted next-exam card, and the vertical gap markers read more clearly than before. Open the gear editor and confirm the modal, row spacing, date fields, and text-action controls still work for save/delete/add/reset flows. |
| 2026-03-26 | Public release hardening | Removed the temporary startup heartbeat before public release, replaced the tracked config with a safe default stub, tightened ignore rules for local-only files, and stripped the public manifests back to the host permissions the public build actually uses. Private live-data overrides now stay local through `config.local.js` plus the local packaging scripts. | Build both browser zips from a clean checkout and confirm they work without any secret/local config file. Verify no request is sent to a usage endpoint on startup, and rerun the tracked-file privacy scan before pushing. |
| 2026-03-22 | Library feed refresh | Flattened the DTU Library events/news list styling to remove several passive pill/card treatments. Event and news rows now read as open list items with divider lines and soft hover surfaces, event dates use a simpler accent rail instead of boxed badges, and news categories use lightweight accent labels instead of rounded chips. | Open the DTU Library panel in light mode and dark mode. Verify `Upcoming Events` and `Library News` items no longer look like stacked rounded cards/pills, and confirm hover states remain subtle while titles and metadata stay easy to scan. |
| 2026-03-22 | DTU Learn accent reach | Fixed the accent navigation band on legacy Learn course-tool pages like Dropbox/Classlist/Group/News, where the normal full accent sweep is intentionally skipped for responsiveness. A new lightweight band-only pass now walks the relevant open shadow roots and styles `d2l-labs-navigation-band` hosts directly, so the top band keeps the chosen accent color even on those legacy routes. | Open a legacy Learn tool page such as `/d2l/lms/dropbox/user/folders_list.d2l?...` with dark mode OFF and verify the top navigation band is accent-colored rather than the default DTU color. Also confirm the page still stays responsive, since only the targeted band pass should run on these legacy pages. |
| 2026-03-22 | Library menu refresh | Restyled the DTU Library quick-links area into a flatter `Library Menu` section with accent SVG icons, secondary helper text, and lighter hover motion instead of pill-like bordered buttons. The menu now relies on spacing and subtle surface shifts rather than boxed link cards, while staying readable in both light mode and dark mode. | Open the DTU Library panel in light mode and dark mode. Verify the `Library Menu` links render as an open menu with neutral text, accent-colored icons, and no heavy per-link borders, and confirm hover states only use a subtle surface tint plus a small lift/arrow motion. |
| 2026-03-22 | DTU Learn accent reach | Fixed the DTU Learn navigation band accent pass for nested shadow DOM cases. When `d2l-labs-navigation-band` is itself a shadow-root host, the runtime accent pass now styles the host directly, so the band keeps the chosen accent color even with dark mode OFF. | On a Learn page with dark mode OFF, verify the main top navigation band uses the chosen accent instead of the default DTU color, including cases where the band is rendered through nested Brightspace shadow roots. |
| 2026-03-22 | DTU Learn light-mode cleanup | Fixed another DTU Learn light-mode regression where the top course navigation and course title could inherit the global accent-link color. Brightspace top-nav anchors are now excluded from the generic always-on accent rule, and the Learn light-mode runtime pass re-neutralizes nav labels/course title to black. | On a Learn course page with dark mode OFF, verify `Content`, `Video Recordings`, `Discussion Forum`, `Quizzes`, `Surveys/feedback`, `Assignments`, and the course title render in neutral black rather than the chosen accent color. Then switch dark mode ON and confirm the title/nav still follow the intended dark-theme styling. |
| 2026-03-22 | DTU Learn light-mode cleanup | Fixed a Learn legacy LMS regression where Dropbox/Classlist/Group/News pages could still receive inline dark background overrides even when dark mode was off. The legacy runtime repaint now only applies in dark mode and removes those background overrides in light mode so assignment rows stay native/light. | Open `learn.inside.dtu.dk/d2l/lms/dropbox/...` with dark mode OFF and verify assignment rows, due-date blocks, and status cells no longer get inline `rgb(45,45,45)` backgrounds. Then switch dark mode ON and confirm the same legacy pages still darken correctly. |
| 2026-03-04 | Live bus resilience | Fixed a regression in the optional fast-path bus fetch where a temporary widget-route failure could still blank the homepage bus widget even though the slower per-stop fallback path was healthy. The extension now falls back to the legacy per-stop fetch path whenever either the optional fast path or a direct API key is available, instead of requiring a direct Rejseplanen key just to recover from transient fast-path issues. | Reload the extension, enable bus departures, and verify the homepage still shows departures if the fast path is flaky. A temporary widget error should now fall back to the per-stop path rather than ending in `No upcoming buses`. |
| 2026-03-04 | Live bus resilience | Hardened the optional fast-path bus widget against stale or over-specific saved direction filters. If the widget route returns `ok` but no departures for a config that uses specific directions, the extension now retries once with `All directions` before giving up, which prevents the homepage bus area from going blank just because old saved filters no longer match current Rejseplanen labels. | On DTU Learn with bus departures enabled, save a narrow direction filter that no longer matches current naming and verify the widget still recovers departures via the all-directions retry. Also confirm normal strict filters still work when they do match current directions. |
| 2026-03-04 | Live data recovery | Restored the missing library crowding helper functions in `background.js` after the branch split. The live library-stats message path regained its URL sanitizing, live fetch, and normalization code instead of calling missing helpers at runtime. | Reload the extension, open the library panel, and verify the chart/header update from the live source again instead of silently failing. |
| 2026-03-04 | Live data recovery | Restored the optional live-data wiring after the cleanup branch split. Bus departures now use an optional configured fast path when available, and the library crowding panel prefers an optional live-data backend before falling back to direct DTU occupancy scraping. | With a local live-data override configured, verify DTU Learn bus departures load again and the library panel shows fresh crowding data instead of stale `---` placeholders. If the optional backend is unavailable, confirm the library panel falls back gracefully and the bus widget does not crash. |
| 2026-03-04 | Emergency pause control | Moved the `Save rule` action up into the same row as the paused-URL input field so adding a rule reads as one compact action instead of a separated button below the suggestions/status area. | Open the `Paused URLs` modal and verify the input field and `Save rule` action sit on the same line, with the status message remaining below. |
| 2026-03-04 | Emergency pause control | Protected only the root DTU Learn home entry (`/d2l/home`) from pause rules so users cannot disable the main Settings/Admin entry point. Course home pages like `/d2l/home/296283` can still be paused if needed. The paused-URL modal now rejects only the protected root rule with a clear message. | Open the paused-URL modal on Learn, try to save `https://learn.inside.dtu.dk/d2l/home`, and verify it is rejected with an explanatory message. Then save a course homepage like `https://learn.inside.dtu.dk/d2l/home/296283` and verify that specific page can still be paused. |
| 2026-03-04 | Emergency pause control | Flattened the `Paused URLs` modal by removing the boxed section styling around `Add paused URL rule` and `Saved paused URL rules`, removed the separate `Use` button from suggested rules, and updated the examples to recommend valid pause targets instead of the protected root home URL. | Open the paused-URL modal and verify the add/saved sections no longer sit inside bordered boxes. Confirm suggested rules are plain clickable rows without a separate `Use` action, and that the helper text uses valid examples such as `/d2l/home/296283` or a lesson/tool URL. |
| 2026-03-04 | Settings/Admin footer actions | Simplified the disclaimer footer actions again: the feedback link and pause manager now sit in one horizontal row, and the separate `Pause...` / `Manage paused URLs...` entries were merged into a single `Paused URLs...` action that opens the shared blurred modal. | Open Settings and verify the footer shows one single-line action row with `Send feedback or bug report` and `Paused URLs...`. Confirm the pause modal still opens correctly from the merged action. |
| 2026-03-04 | Emergency pause control | Replaced the prompt-based pause manager with a proper blurred `Paused URLs` modal. The modal combines add/remove in one place, shows suggested rules for the current page, lists saved rules with plain-text `Remove` actions, and keeps the styling flatter instead of using pill-like controls. | Open Settings, click either pause action, and verify a blurred modal opens. Add a new rule from the suggested list, remove an existing rule from the saved list, and confirm the current page reloads when the added/removed rule affects it. |
| 2026-03-04 | Settings/Admin footer actions | Hardened the footer action styling so `Send feedback or bug report` and `Paused URLs...` keep a transparent background even when broader site/button styling tries to paint them. The footer actions now force transparent background, no border, and no shadow across normal, hover, active, and focus states. | Open the Settings/Admin footer after reloading the extension and verify both actions render as plain text only, with no filled dark-2 style behind them in normal, hover, pressed, or focused states. |
| 2026-03-04 | Emergency pause control | Added an emergency URL-pause system for DTU After Dark. Matching pages short-circuit very early and show a small `Resume` banner instead of running the normal extension logic, while the Settings/Admin footer exposes the pause manager for adding and removing saved rules. | Open Settings on a normal page, add a non-home Learn URL rule such as a lesson or legacy tool page, then reload a matching page and verify the extension stays paused there with only the `Resume` banner visible. Click `Resume` and confirm the matching pause rule is removed and the page reloads with the extension active again. |
| 2026-03-04 | DTU Learn legacy tool styling | Extended the dark-2 legacy LMS repaint beyond Dropbox rows to also cover old Classlist/Groups/News inline action links such as participant email links (`EmailUser(...)`) and `Show Search Options` (`#z_eh` / `ToggleSearch(...)`). The same runtime repaint path now runs across the broader legacy `/d2l/lms/(dropbox|classlist|group|news)/...` family. | On legacy Learn pages, verify inline links like participant names with `Compose email` actions and the `Show Search Options` link render on dark 2 instead of dark 1/bright backgrounds, and stay correct after rerender/postback. |
| 2026-03-04 | DTU Learn legacy tool stability | Expanded the lightweight Learn-page path beyond legacy Assignments/Dropbox to also cover legacy Classlist, Groups, and News routes under `/d2l/lms/*`. These pages now skip the heaviest Brightspace shadow-DOM sweeps, unified mutation observer, accent shadow pass, and smart-room scans that could otherwise make them hang or trigger `Page unresponsive`. | Open legacy Learn course tools such as `/d2l/lms/classlist/classlist.d2l?ou=...`, `/d2l/lms/group/user_group_list.d2l?ou=...`, and `/d2l/lms/news/main.d2l?ou=...` with the extension enabled. Verify they load normally and stay responsive, while ordinary Learn homepage/course pages still keep their existing styling/features. |
| 2026-03-04 | Repository privacy | Removed the private live-data backend from the GitHub-facing repo state and prepared the project so the private variant can stay local-only instead. Public history is being rewritten without the private infra/docs/live endpoint references. | Verify the public branch no longer contains private infra/docs/live endpoint strings, while the local-only branch remains available for private use. |
| 2026-03-04 | CampusNet GPA tools | Normalized the `Hide/Back` excluded-state styling so the control stays in the same accent family instead of switching to a different green color. Excluded rows now also get runtime inline mute/strike styling, which fixes the missing line-through behavior in light mode. | On `campusnet.dtu.dk/cnnet/Grades/*`, exclude a row in both dark mode ON and OFF. Verify `Hide` changes to `Back` with only a dimmer same-family accent treatment, not a different hue, and confirm the excluded row text is struck through and muted in light mode as well. |
| 2026-03-04 | Study Planner exam editor | Removed the duplicated `Edit timeline entries` heading/subtitle inside the blurred exam-editor modal. The modal now owns the outer title block, while the reused inner timeline-editor component hides its own heading when embedded there. | On `studieplan.dtu.dk`, open the exam editor and verify the modal shows only one `Edit timeline entries` title block at the top instead of repeating the same heading again inside the panel. |
| 2026-03-04 | CampusNet GPA tools | Moved the `Hide/Back` code-column alignment into the runtime CampusNet GPA path as inline layout, so light mode now gets the same fixed first-column width and right-aligned control lane as dark mode. | On `campusnet.dtu.dk/cnnet/Grades/*` with dark mode OFF, verify `Hide`/`Back` sits in the same aligned lane between course code and title as it does in dark mode, instead of sticking directly to the course code text. |
| 2026-03-04 | Study Planner exam editor | Removed accent-colored styling from the exam-editor gear icon and action links. The gear now uses neutral theme color only (`white` in dark mode, dark text in light mode), while `Save`/`Add`/`Restore` use stable semantic success color and `Delete` keeps stable danger color, independent of the chosen accent theme. | On `studieplan.dtu.dk`, verify the gear icon is white in dark mode and dark in light mode rather than accent-colored. Open the editor and confirm `Save`/`Add`/`Restore` stay readable and do not change with custom accent themes, while `Delete` remains a stable danger color. |
| 2026-03-04 | Repository privacy | Added `data/` to `.gitignore` and removed the tracked `data/rooms_spring_2026.json` file from the Git index so local room/course mapping data no longer gets included in future commits or GitHub pushes by default. | Verify `git status` shows the room JSON as removed from the index and that future files under `data/` stay untracked. If the file was already pushed earlier, remove it from repository history separately because `.gitignore` only stops future tracking. |
| 2026-03-04 | Study Planner exam editor | Moved the gear-opened exam editor out of the inline `Exam Schedule & Gaps` widget and into a blurred modal with the same backdrop treatment as other extension modals. Flattened the editor UI at the same time: `Save`/`Delete`/`Reset edits`/`Close` are now text-style actions, exam-choice options are less pill-like, and row shells like `02451 · Wed, May 27` now use simple divider/left-border styling instead of rounded cards. | On `studieplan.dtu.dk`, click the exam-widget gear and verify a popup/modal opens with the usual blurred page backdrop while the normal timeline stays in place behind it. Confirm `Save`/`Delete`/`Reset edits`/`Close` render as text actions, existing/custom rows no longer look like pills/cards, and edit/save/delete/custom-entry flows still work. |
| 2026-03-04 | CampusNet GPA tools | Hardened the plain-text `Hide/Back` control again by forcing transparent background, no border radius, no box shadow, and native-button appearance reset in both runtime inline styling and CSS. This prevents inherited button chrome from painting any accent-colored block behind the text. | On `campusnet.dtu.dk/cnnet/Grades/*`, verify `Hide`/`Back` shows only accent-colored text on a fully transparent background in both normal and hovered states, with no green/filled rectangle behind the label. |
| 2026-03-04 | CampusNet GPA tools | Removed the remaining inline background/border paint from the plain-text `Hide/Back` control, so only the text carries the accent color and the background stays fully transparent in all states. | On `campusnet.dtu.dk/cnnet/Grades/*`, verify `Hide`/`Back` no longer shows any accent-colored block behind the text, and that only the text itself is accent-colored by default. |
| 2026-03-04 | Study Planner exam timeline | Reduced the rounded-card feel in `Exam Schedule & Gaps` by tightening panel/card corner radii while keeping the same structure, accent border cues, and risk coloring. | On `studieplan.dtu.dk`, open `Exam Schedule & Gaps` and verify the warning summary box and exam cards look flatter/less rounded than before, without losing readability. |
| 2026-03-04 | Study Planner exam timeline | Reduced pill-heavy styling in `Exam Schedule & Gaps`: `Tightest gap`, `Next up`, and the inline gap markers now render as cleaner text labels instead of rounded capsules, while keeping the same risk colors and timeline structure. | On `studieplan.dtu.dk`, open `Exam Schedule & Gaps` and verify the top `Tightest gap` indicator, the hero `Next up` label, and gap labels like `4d gap`/`26d gap` appear as plain text treatments rather than pills. |
| 2026-03-04 | CampusNet GPA tools | Dimmed the plain-text `Hide/Back` controls slightly so they read more like secondary actions, while keeping white hover feedback and the aligned code-column layout. | On `campusnet.dtu.dk/cnnet/Grades/*`, verify `Hide`/`Back` is a little less bright by default, still readable, and turns brighter on hover. |
| 2026-03-04 | CampusNet GPA tools | Reworked the official-grade row control layout in the course-number column: the code column now has a fixed width and the `Hide/Back` control floats to the right edge of that column as plain text, so the controls align in one vertical lane between course code and course title without pill styling. Also kept the one-click responsiveness fix after moving the control into the code column. | On `campusnet.dtu.dk/cnnet/Grades/*`, verify all `Hide` controls align in the same vertical column between course code and title, render as plain text (not pills), change to `Back` on first click, and update GPA/ECTS calculations immediately. |
| 2026-03-04 | CampusNet GPA tools | Added ignore/restore controls on official CampusNet grade rows so users can exclude courses that should not count toward their own GPA/ECTS view (for example after switching study lines). Weighted GPA, projected GPA, and ECTS progress now all recalculate from the filtered official rows while keeping hypothetical-grade rows separate. | On `campusnet.dtu.dk/cnnet/Grades/*`, verify each official grade row gets an `Ignore` button on the row. Ignore one or more official rows and confirm the row becomes muted/struck-through, `Weighted GPA` updates, the ECTS bar updates, and `Projected GPA` also respects the ignored rows. Reload the page and confirm the ignored-row choice persists. |
| 2026-03-04 | DTU Learn mobile navigation | Tightened the mobile header repaint to cover direct child blocks of `.d2l-navigation-s-mobile-menu-header` and `.d2l-navigation-s-mobile-menu-branded-header`, so transparent/flex gaps in the logo/header row no longer show the drawer's dark-1 background through the intended dark-2 strip. | Open the DTU Learn mobile menu and verify the full header row, including gaps between logo/gutter/course-selector/back-button blocks, stays dark 2 with no dark-1 bleed-through. |
| 2026-03-04 | DTU Learn mobile navigation | Moved the mobile-menu header containers (`.d2l-navigation-s-mobile-menu-header`, `.d2l-navigation-s-mobile-menu-branded-header`, and `.d2l-navigation-s-gutter`) from dark 1 to dark 2 so the whole header/logo strip is consistent instead of split across two dark tiers. | Open the DTU Learn mobile menu and verify the full top header strip around the logo/course selector/back button is dark 2, while the main drawer body below remains dark 1. |
| 2026-03-04 | DTU Learn mobile navigation | Moved the DTU logo header area (`.d2l-navigation-s-header-logo-area.d2l-navigation-s-header-no-home-icon`) from dark 1 to dark 2 inside the mobile drawer so the logo margin strip matches the surrounding utility/header treatment instead of the main drawer shell. | Open the DTU Learn mobile menu and verify the margin/background area around the DTU logo is dark 2, while the main drawer body remains dark 1. |
| 2026-03-04 | DTU Learn mobile navigation | Darkened the DTU Learn mobile navigation drawer inside Brightspace shadow DOM. The drawer shell/header surfaces now use dark 1, while close/control strips and small utility surfaces use dark 2, with runtime inline re-application to survive Brightspace repaints. | Open the DTU Learn mobile menu and verify the drawer background/header are dark 1, the utility/control strips are dark 2, the accent band stays accent-colored, and the menu remains readable after reopen/rerender. |
| 2026-03-03 | DTU Learn floating buttons | Strengthened the floating action-buttons surface styling to force dark 1 (`#1a1a1a`) on `.d2l-floating-buttons-container` and `.d2l-floating-buttons-inner-container`, including shadow-root injected CSS with `background`, `background-color`, and `background-image` reset. | On a DTU Learn page with floating action buttons inside Brightspace shadow DOM, verify the floating-buttons wrapper/background is dark 1 and does not keep a light or transparent strip. |
| 2026-03-03 | DTU Learn assignments styling | Extended the legacy Assignments/Dropbox dark-2 repaint to submission-history links (`folders_history.d2l`) and due-date wrappers/text (`.d2l-folderdates-wrapper`, `.d2l-dates-text`) so the full assignment metadata row stays on the same dark 2 surface. | On a legacy Assignments page, verify `1 Submission, 2 Files` links and due-date blocks like `Due on 26 February, 2026 17:00` use dark 2 backgrounds and remain correct after refresh/rerender. |
| 2026-03-03 | DTU Learn assignments styling | Darkened legacy DTU Learn Assignments/Dropbox title/status rows to dark 2 (`#2d2d2d`), including the folder-name containers and submit/status links such as `Not Submitted`, with both stylesheet selectors and runtime inline re-application for stubborn legacy markup. | Open a legacy Assignments page and verify the assignment title row (`.d2l-foldername`) and submit/status links (`folder_submit_files.d2l`, including `Not Submitted`) render with dark 2 backgrounds in dark mode, and stay stable after page refresh/rerender. |
| 2026-03-03 | DTU Learn assignments stability | Added a lightweight runtime path for legacy DTU Learn Assignments/Dropbox pages (`/d2l/lms/dropbox/*`). These pages now skip the deepest Brightspace shadow-root retries, unified mutation observer, accent shadow sweep, and smart-room scans that could make assignment lists hang or trigger `Page unresponsive`. | Open a course Assignments page such as `https://learn.inside.dtu.dk/d2l/lms/dropbox/user/folders_list.d2l?ou=296283&isprv=0` and confirm it loads normally with the extension enabled. Verify the page stays responsive in dark mode and that regular DTU Learn homepage/course pages still keep their existing dynamic styling/features. |
| 2026-03-03 | Kurser prerequisite helper | Added a new `kurser.dtu.dk` prerequisite/workload helper panel powered by the official DTU course webservice (`course.asmx/GetCourse`). The panel shows teaching hours, teaching-team mix, and parsed prerequisite course-code groups with linked related courses when the API exposes structured prerequisite data. | On a course page with structured prerequisite data (for example `22100` in `2025/2026`), verify the new panel appears under the title/grade stats, shows `Teaching hours`, lists parsed prerequisite logic with `AND/OR`, and links the related course codes. Confirm it stays readable in both dark mode ON and OFF and can be disabled via `Prerequisite Helper (Kurser)` in settings. |
| 2026-02-27 | DTU Learn onboarding hint | Reworked the first-run Learn homepage onboarding bubble so it points to the injected `Settings` nav entry first (with fallback to the admin gear only if needed), and changed it from the old 3-visit generic reminder into a one-time first-install hint for feature toggles. Added a short retry window so the hint still appears when the nav renders late. | On a fresh profile/install, open the DTU Learn homepage and verify a single onboarding bubble points to `Settings` (or the gear if Settings is unavailable). Dismiss it and confirm it does not reappear on later visits. |
| 2026-02-27 | DTU Learn bus direction picker | Removed `Rævehøjvej, DTU` direction options from all lines except `40E`, and removed `Risø` from `600S` in the bus direction picker. Applied at the shared direction-option filter layer so both static presets and cached/fetched direction labels are cleaned consistently. | In Bus `Edit` -> `Pick Directions`, verify `150S`/`15E` no longer show `Rævehøjvej, DTU`, `600S` no longer shows `Risø`, and `40E` still keeps its Rævehøjvej terminal direction. |
| 2026-02-27 | Study Planner exam editor | Polished the exam timeline editor: replaced the off-center gear with a centered accent gear icon, restored a native calendar picker while keeping `DD/MM/YYYY` text display in date fields, kept official/custom edit rows sorted chronologically, and capped editor-height growth so the widget stays compact while open. | On `studieplan.dtu.dk`, open the exam widget editor and verify the gear icon hole is visually centered, the date field still opens a calendar picker, rows are sorted earliest-to-latest, and the open editor scrolls instead of making the whole widget excessively tall. |
| 2026-02-25 | DTU Learn Library crowding copy/time formatting | Refined crowding readability: forced 24-hour `Updated` time (`da-DK`), added small explainer text under `Now/Today/Free` values, added `% filled` micro-label next to occupancy percent, and clarified chart-note coverage when the typical band has not reached later evening windows yet. Also extended typical-band drawing to hour boundaries to avoid abrupt half-hour cutoffs. | Verify header timestamp renders like `17:59` (not PM/AM), hero stats show helper text, occupancy percent shows `filled`, and chart note explains partial-band coverage when applicable. |
| 2026-02-25 | DTU Learn homepage navigation | Removed the native `Help` dropdown from the DTU Learn homepage top navigation by filtering out the `Help` nav item during feature checks (re-applies after dynamic nav rerenders). | On `/d2l/home`, verify `Help` is no longer shown in the main nav row after load and after in-page rerenders. |
| 2026-02-25 | DTU Learn Library crowding hero layout | Moved `Now / Today / Free` metrics from the panel header into the occupancy hero row (to the right of `% + status`, above the progress bar), and kept header right-side actions as `Updated + Refresh + Close` only. | Open Library panel and verify top header no longer shows metric chips; confirm hero row shows `% + Moderate` on the left and `Now / Today / Free` on the right above the horizontal bar. |
| 2026-02-25 | DTU Learn Library chart overlays legend | Added explicit event legend under the crowding chart for shaded overlays: each row now shows `time interval Â· event title` for DTU Library events rendered on the chart, so users can see what each highlighted span means. | Open Library panel on a day with DTU Library events and confirm each shaded chart interval has a matching legend row with the correct time range and event title. |
| 2026-02-25 | DTU Learn Library chart overlays | Added event-span overlays on the crowding chart using library calendar API timing/location metadata. The chart now shades time intervals for same-day events only when event location contains `DTU Library` and appends highlighted intervals to the chart note text. | Verify only `DTU Library` events create overlays, and that intervals (for example `16:45-20:00`) align with the x-axis time range for today. Confirm non-library-location events do not render overlays. |
| 2026-02-25 | DTU Learn Library crowding widget compactness | Further compacted the crowding hero: percentage and status now sit inline (`66%  Moderate`), removed the duplicate bottom occupancy percentage row, and hid the `Crowding Trend` heading text so the section starts directly with content. | Open Library panel and verify no duplicate percentage appears, hero is visually shorter, and the section no longer shows a `Crowding Trend` title label. |
| 2026-02-25 | DTU Learn Library crowding widget copy/layout | Refined V2 crowding presentation after live review: compacted the hero card size, switched hero headline from duplicated `Free seats` to `Occupancy now` percent, removed standalone insight row (insight now appended to chart note), and simplified confidence copy to avoid `shared` wording (`Based on about X day(s) of collected data`). | Verify Library panel no longer shows large duplicate free-seat emphasis in trend hero, insight is integrated into chart note, and confidence line avoids the word `shared`. |
| 2026-02-25 | Library trend copy | Simplified trend wording: removed `(Shared)/(Personal)` title suffix from the panel heading (`Crowding Trend` only), changed phrasing to `Typical right now`, `Busiest recent hours`, `Quieter recent hours`, and `Data confidence` for clearer/non-technical language. | In the Library modal trend section, verify the heading is just `Crowding Trend` and the insight text uses the new phrasing without basis suffixes. |
| 2026-02-25 | Study Planner exam panel copy | Renamed panel heading from `Exam Timeline and Clash Risk` to `Exam Schedule & Gaps` for a clearer, less alarm-heavy label while keeping the same timeline/gap logic. | On `studieplan.dtu.dk`, verify right-side exam panel title shows `Exam Schedule & Gaps` and all existing risk/gap indicators still render unchanged. |
| 2026-02-25 | Library trend UI | Moved `Crowding Trend` above `Quick Links`, added an accent-colored shared hourly line chart (only shown after confidence threshold is met), and hardened insight text so busiest/quieter comparisons are hidden until multiple distinct hour windows exist (prevents nonsense like same hour being both busiest and quietest). | In Library modal, verify trend section appears above quick links; confirm no chart appears early in data collection; after enough data, confirm accent line chart appears and busy/quiet text uses distinct hour windows only. |
| 2026-02-25 | Studieplan semester headings | Ensured semester heading side borders (`.inverse h3` / `h3[data-bind*="SemesterNumber"]`) use accent color (`var(--dtu-ad-accent-deep)`) instead of legacy red values when dark mode is OFF. Applied in both stylesheet and runtime inline paint path for consistency. | On `studieplan.dtu.dk` with dark mode OFF, verify semester headers (for example `1. Term`) show accent-colored left/right borders and no red border remnants. |
| 2026-02-25 | DTU Learn Library panel UI | Refined Library modal header and quick links: occupancy stats now live near the `DTU Library` title with larger numeric emphasis + muted labels, while `Updated` + `Refresh` + close stay on the right. Quick links were compacted (denser 3x2 grid, lower height/padding) and left-aligned with small icons; `Library News` now matches all other quick-link buttons (no special accent treatment). | Verify header visual separation (info left/actions right), quick-link density/readability, and that `Library News` button style matches the rest in dark and light modes. |
| 2026-02-25 | DTU Learn Library panel | Added live DTU Library occupancy chips (`Now`, `Today`, `Free`) in the panel header next to `DTU Library`. Switched data source to `findit.dtu.dk/screen_data.json?zone=main` (with HTML fallback) to avoid `parse_error` from placeholder values on `/screen?seats=800`. Added auto-refresh every 60 seconds while panel is open. | On DTU Learn, open `Library` panel and verify header occupancy chips render (not error), then confirm values/timestamp refresh about every minute. |
| 2026-02-25 | DTU Learn Library panel trends | Added client-side occupancy history/trend section (`Crowding Trend (Personal)`) that stores local occupancy samples and surfaces typical crowding for the current time plus busiest/quietest hour windows based on collected user data. | Open Library panel over multiple sessions and verify trend section progresses from "collecting data" to meaningful hourly patterns after enough samples are gathered. |
| 2026-02-24 | Studieplan create-activity modal | Added runtime exemption/cleanup for `#createActivity` (and `#searchCourseCatalogDialog`) so global inline dark paint no longer re-applies modal dark backgrounds; removes existing inline `background*` on modal container/dialog/content/header/body/footer. | On `studieplan.dtu.dk`, open `Create` and `Quick-search courses`; confirm neither is forced to dark modal background even after re-open/re-render. |
| 2026-02-24 | Studieplan create-activity modal | Scoped override for `#createActivity` to disable the same global forced dark modal background rule (`.modal, .modal__dialog, .modal__content, .modal__header, .modal__footer, .modal__body { background-color: #2d2d2d !important; }`) only for that dialog. | Open `Create` activity modal on `studieplan.dtu.dk` and verify it is no longer forced to dark-2 while other modals remain unchanged. |
| 2026-02-24 | Studieplan quick-search modal | Scoped override for `#searchCourseCatalogDialog` to disable the global forced dark modal background rule (`.modal, .modal__dialog, .modal__content, .modal__header, .modal__footer, .modal__body { background-color: #2d2d2d !important; }`) only for that dialog. | Open `Quick-search courses` on `studieplan.dtu.dk` and verify this modal is no longer forced to dark-2 while other modals remain unchanged. |
| 2026-02-24 | Studieplan dark-mode stability | Rolled back the `#searchCourseCatalogDialog` force-light patch (both CSS and runtime JS). This restores normal dark-mode rendering on `studieplan.dtu.dk` and removes the modal-specific white repaint logic that caused regressions. | Reload extension and verify Studyplanner page + quick-search modal render in dark mode again, including form controls. |
| 2026-02-24 | DTU Learn course-card Content shortcut | Replaced the blue book emoji icon on the course-card Content shortcut button with a monochrome SVG book glyph that follows button text color (white in dark mode), so the icon stays neutral/consistent across platforms. | On DTU Learn homepage course cards, verify the Content shortcut shows a black/white-style outline icon (no blue emoji) and still opens the same target/edit flow. |
| 2026-02-24 | DTU Learn file links | Excluded `a.d2l-link`/`a.d2l-link-small` file-download links (`href*="/d2l/common/viewFile.d2lfile"`) from the dark-2 breadcrumb-link background rule so direct file links (for example `.ipynb`) keep native link background. | On DTU Learn pages containing file links, verify links like `exercise1.ipynb` no longer get the boxed dark-2 background while breadcrumb links still do. |
| 2026-02-24 | DTU Learn file upload area | Added a force override for `.d2l-fileinput-add` to use background color `#f70606` so the upload drop/add panel matches requested styling even when Brightspace/global rules compete. | On DTU Learn pages with file upload controls, verify the drop/add panel renders red in dark mode and remains readable. |
| 2026-02-23 | DTU Learn lessons sidebar | Disabled Brightspace legacy left-pane separator gradient/right border and selected-item pseudo-overlay; added runtime fallback to keep it disabled after dynamic repaints. | Verify on legacy Content pages that no light separator/overlay remains and selected rows still look intentional in dark mode. |
| 2026-02-23 | DTU Learn course-card Content shortcut | Added per-course custom target overrides for `Go to Content`. Users can `Ctrl/Cmd+Shift+Click` a content button to edit/reset its link. Added Settings edit manager (`Settings -> Dashboard -> Content Shortcut -> Edit`) to view/add/remove/clear overrides. | Test default behavior (no override), per-course add/edit/remove, clear-all, and invalid URL rejection. Confirm navigation still works for normal click. |
| 2026-02-23 | DTU Learn bus widget | Added campus-based stop selection (DTU Lyngby / DTU Ballerup / DTU Risø) with multi-campus combinations during initial setup, plus campus editing inside the Bus modal. Expanded supported line catalog for selected campuses and extended Lyngby stop set (including Fysikvej + Akademivej stops) so line `193` can be configured reliably. Direction setup now supports `All directions` fallback, so lines can be added even when there are no live departures at setup time. | Verify first-time setup flow (single/multi campus), existing-user edit flow, and line filtering per campus. Specifically confirm `193` can be added from Lyngby and departures render with wildcard direction selection. |
| 2026-02-23 | DTU Learn content-shortcut UX | Clarified Add Override input semantics to use Brightspace Org Unit ID (not DTU course code). Add Override now accepts pasted Learn URLs and auto-extracts Org Unit IDs, with warning when a likely DTU 5-digit course code is entered directly. Modal labels now say `Org Unit` to reduce ambiguity. | Validate Add Override with `/d2l/home/{id}`, `/d2l/le/lessons/{id}`, and full Learn URLs. Verify warning appears for plain 5-digit input and that existing 6+ digit IDs still work without friction. |
| 2026-02-23 | DTU Learn bus widget | Reduced configured-line cap back to 2 to limit Rejseplanen API usage. In `+ Add Bus Line`, each line now shows campus tags (for example `DTU Lyngby` / `DTU Ballerup`) based on selected campuses, and the picker subtitle now shows selected campus scope. Marked Bus modal overlay/content as extension UI so global dark-mode repainting does not hide line badge colors while selecting lines. | Validate cap enforcement at 2 lines, then verify `+ Add Bus Line` only lists lines from currently selected campuses and shows correct campus tags (including shared lines like `40E`). With dark mode ON, confirm line badge colors remain visible in Add Line and direction setup. |
| 2026-02-23 | DTU Learn modals | Matched backdrop blur behavior in Bus `Edit` modal and Content Shortcut `Edit` manager to the Settings/Admin modal (same transparent + `blur(4px)` treatment, removed extra brightness-heavy blur). | Open Settings/Admin, Bus `Edit`, and Content Shortcut `Edit` back-to-back and confirm the page backdrop blur intensity now looks identical across all three. |
| 2026-02-23 | Settings/Admin information architecture | Rebalanced Settings/Admin submenu grouping: `Interface` is now `Shortcuts & Navigation` and includes Navigation Quick Links, Content Shortcut, Library, and Smart Room Links. `Dashboard` renamed to `Dashboard Widgets` and now focuses on homepage widgets/tools. `Course Catalog` keeps kurser/course-resource tools without Smart Room Links. Applied in both standalone Settings modal and legacy Admin-tools restructure path for consistency. | Verify category labels/order and that each moved toggle is present, functional, and still opens associated edit actions (`Bus -> Edit`, `Content Shortcut -> Edit`). |
| 2026-02-23 | DTU Learn bus widget | Hardened bus-line route chip colors in Bus `Edit` flow (Add Line list, configured-line list, and direction header) with inline `!important` runtime overrides, so dark-mode global button/text CSS cannot wash out route colors. | With dark mode ON, open Bus `Edit` and confirm route chips (`150S`, `300S`, etc.) keep distinct colors in all bus-config steps. Re-check light mode for no regression. |
| 2026-02-23 | Study Planner top bar | Fixed dropdown caret rendering on `studieplan.dtu.dk` so caret triangles keep white tip with transparent background (no accent-filled caret block). Removed runtime accent background writes on carets and excluded caret spans from broad `.pull-right > span` accent paint. | In dark mode, open the top-right `Menu` dropdown and confirm the caret has no accent background rectangle while surrounding bar styling remains accent-colored. |
| 2026-02-23 | DTU Learn top navigation notifications | Exempted notification bell and red-dot indicator from wildcard dark background repainting. Bell icon background now stays transparent, and `.d2l-labs-navigation-notification-icon-indicator` now follows the selected accent color. Applied in both global CSS and injected shadow-root styles to cover Brightspace render variants. | In dark mode, check top-nav notification bell: no dark background block behind bell, and unread indicator dot uses current accent color (including custom accent). |
| 2026-02-23 | DTU Learn top navigation notifications | Hardened bell-icon background fix for deep/nested shadow DOM by adding runtime exemptions in `applyDarkStyle`/`applyLighterDarkStyle` and re-applying bell/indicator inline overrides in the accent shadow-root pass. | With dark mode ON, confirm notification bell background remains transparent after live re-renders/navigation changes, while unread dot still follows the selected accent. |
| 2026-02-23 | Settings/Admin disclaimer | Added a small `Debug/Ideas` button in the Settings/Admin disclaimer footer (standalone Settings modal + legacy Admin-tools panel variant). The button opens the user's default mail client via `mailto:` to send feedback/issues without exposing the raw email in the visible UI. | Open both settings entry paths, verify the new button is visible near the disclaimer, and confirm click opens a compose window addressed to the maintainer. |
| 2026-02-23 | Student + Examplan theming coverage | Added `student.dtu.dk` and `eksamensplan.dtu.dk` to extension content-script matches (Firefox + Chrome) so dark mode is injected on those sites. Also added both domains to MV3 web-accessible resource matches and host permissions to ensure stylesheet loading works on Chrome builds. | Reload extension, open both sites, and confirm full dark theme applies. Re-test DTU Learn/CampusNet pages for regressions after manifest host expansion. |
| 2026-02-23 | student.dtu.dk factbox contrast | Forced `.o-factbox` blocks (for example the `Datoer og Deadlines` panel) to Dark 1 (`#1a1a1a`) so homepage factboxes are no longer bright against the dark theme. | On `student.dtu.dk`, confirm factboxes use dark 1 background and keep readable text/link contrast. |
| 2026-02-23 | student.dtu.dk header contrast | Forced `.h-header` (student portal top header block) to Dark 2 (`#2d2d2d`) so the site header matches the extension's dark tiering. | On `student.dtu.dk`, verify the full top header bar is Dark 2 and text/icons remain readable. |
| 2026-02-23 | student.dtu.dk header accent strip | Set `.h-header` top border to active accent color (`border-top: 5px solid var(--dtu-ad-accent)`) so the student portal header keeps a theme-colored top line instead of a fixed color. | On `student.dtu.dk`, verify the top 5px header strip follows selected accent in dark mode. |
| 2026-02-23 | student.dtu.dk social footer accent | Set `.h-footer__social` background to extension accent (`var(--dtu-ad-accent-deep)`) so the social-links footer strip follows the selected theme color. | On `student.dtu.dk`, verify the social footer band behind Facebook/Instagram/LinkedIn/X/Youtube uses selected accent and keeps link contrast. |
| 2026-02-23 | student.dtu.dk logo handling | Disabled extension logo replacement on `student.dtu.dk` so the portal keeps its native inline DTU identity SVG (`#dtu-logo`) instead of the extension `Corp_White_Transparent.svg` asset. | On `student.dtu.dk`, verify the top-right DTU logo uses the site-native SVG and no extension-replaced logo appears. |
| 2026-02-23 | student.dtu.dk badge accent theming | Replaced student-portal purple theme token usage with extension accent for `.u-theme-purple`/`subject-*` theme blocks and forced `.m-badge__kicker` badge chips (including news badge wrappers) to accent background with white text. | On `student.dtu.dk`, verify chips like `Meddelelser til studerende` follow selected accent (not purple) and stay readable in dark mode. |
| 2026-02-23 | student.dtu.dk runtime style hardening | Added `fixStudentPortalStyling()` runtime pass (inline `!important`) and wired it into top-window feature checks so header/factbox/badge/footer/logo styling re-applies after dynamic client-side re-renders. Also added `--color-theme-dark` overrides for purple-theme blocks/footer and header pseudo-element darkening. | After reload, validate on `student.dtu.dk` that header accent strip, factboxes, purple badges, social footer, and native DTU SVG logo remain correct after navigation/search/menu toggles. |
| 2026-02-23 | README wording cleanup | Simplified `student.dtu.dk` interface-enhancement bullets from selector-level details to broad, user-facing summaries (dark theme alignment + accent theming). | Confirm README remains accurate but less noisy, with implementation details kept in code/changes history instead of feature bullets. |
| 2026-02-23 | README feature-list cleanup | Refactored the full `Key Features` section to high-level capability bullets across all subsections (Academic, Course Logistics, Campus Tools, Participant Intelligence, Interface), removing selector-level implementation detail. | Verify README still reflects shipped capabilities while staying concise and release-friendly. |
| 2026-02-23 | student.dtu.dk wrapper/mega-nav contrast | Changed broad page-wrapper/header/footer fallback surfaces (`.page-articlehtml`, `.container_12`, `form`, `header`, `footer`, etc.) from Dark 2 to Dark 1 to prevent bright/accent bleed in student portal mega-navigation contexts. | On `student.dtu.dk`, open mega navigation and verify wrapper backgrounds stay Dark 1 (`#1a1a1a`) rather than red/bright fills. |
| 2026-02-23 | student.dtu.dk gray section contrast | Forced `.u-bg-color--gray` sections (for example the `Aktuelt` band) to Dark 1 (`#1a1a1a`) so gray content bands no longer appear bright in dark mode. | On `student.dtu.dk`, verify gray-section blocks render Dark 1 and keep readable text/cards. |
| 2026-02-23 | student.dtu.dk link-list contrast | Forced `.o-link-list` blocks (for example the `DTU Bibliotek` link list module) to Dark 1 (`#1a1a1a`) to remove bright panel backgrounds in dark mode. | On `student.dtu.dk`, verify link-list cards use Dark 1 while links/icons remain readable. |
| 2026-02-23 | student.dtu.dk embed accent token | Overrode embedded social-card theme token on `div[data-embed-id="2150775"]` so `--tb-bg-color` follows extension accent (`var(--dtu-ad-accent-deep)`) instead of fixed purple. | On the student portal embed card, verify accent areas follow selected accent color and no purple token remains. |
| 2026-02-23 | DTU Learn bus widget API usage | Removed live direction-discovery calls from the Bus `Edit` modal. Direction choices are now built from local/static tokens plus cached data, and bus polling is paused while the modal is open and resumed on close. | Open Bus `Edit` and confirm no Rejseplanen calls are made just by opening/stepping through direction selection. Verify departures refresh again after closing the modal. |
| 2026-02-23 | student.dtu.dk main wrapper contrast | Forced `.js-main-wrapper.h-page__wrapper.h-page__wrapper--fullwidth` to Dark 1 (`#1a1a1a`) so the full-width page wrapper cannot stay bright or inherit incorrect backgrounds. | On `student.dtu.dk`, inspect the main page wrapper and confirm it resolves to Dark 1 in dark mode. |
| 2026-02-23 | student.dtu.dk badge accent targeting | Switched badge accent targeting from kicker-only selectors to container selectors: `.m-badge, .r-badge` now receive accent background, and the previous `.u-theme-purple .m-badge__kicker, .o-news__badgeWrapper .m-badge__kicker` force rule was removed. | On `student.dtu.dk`, verify badges like `Anmeld fejl og fare` show accent on the badge container without the old kicker-only override fighting layout/theme. |
| 2026-02-23 | student.dtu.dk support gate | Temporarily disabled `student.dtu.dk` from release execution by removing its content-script matches (Firefox + Chrome) and Chrome web-accessible-resource match. Kept host permissions for student-domain data-source fetches used by other features. | Confirm extension no longer injects/runs on `student.dtu.dk`, while deadline/exam source fetches from Learn tools still work. |
| 2026-02-24 | DTU Learn notification icon surface | Exempted `d2l-labs-navigation-notification-icon` from dark wildcard background repaint (including shadow-host traversal in runtime styling), so the notification icon container no longer gets forced `#2d2d2d` backgrounds. | On the DTU Learn homepage top nav, verify notification icon area stays transparent while the unread dot remains accent-colored. |
| 2026-02-24 | DTU Learn bus direction presets | Replaced bus-direction hardcoded placeholders with exact destination names fetched from Rejseplanen `linematch` for DTU-supported lines (`150S`, `300S`, `40E`, `15E`, `193`, `350S`, `55E`, `216`, `600S`). | In Bus `Edit` -> direction selection, verify each line shows real destination names matching current Rejseplanen naming. |
| 2026-02-24 | Studyplanner basket action layout | Moved `Create`, `Find course`, and `Export to PDF` action block into the `Basket` header area (right side), with re-render-safe repositioning on `studieplan.dtu.dk`. | In Studyplanner, confirm these actions sit to the right of `Basket` and remain there after dynamic updates/navigation within the page. |
| 2026-02-24 | Studyplanner course-search modal override | Disabled forced modal dark-surface repaint for `#searchCourseCatalogDialog` (Quick-search courses) by excluding it from runtime dark painters and overriding modal background selectors for that dialog only. | Open `Find course` on Studyplanner and verify this modal no longer inherits the global `.modal/.modal__*` forced background styling. |
| 2026-02-24 | Studyplanner quick-search form wrapper override | Disabled the broad dark-1 `form` wrapper repaint for `#searchCourseCatalogDialog #searchForm` so the quick-search form no longer inherits the global `.page-articlehtml, .container_12, form, ...` background rule. | In Studyplanner `Find course`, verify the form container/background is no longer forced by the global `form` dark-1 rule. |
| 2026-02-24 | Studyplanner quick-search modal hard override | Added a hard scoped reset for `#searchCourseCatalogDialog` (CSS + runtime inline re-apply) to override persistent global dark inline styling in modal surfaces, form wrappers, controls, and table rows. | Open `Find course` after reload and confirm the modal/form no longer appears darkened by the generic modal/form repaint rules. |
| 2026-04-21 | Repo cleanup | Extracted the DTU Learn deadlines homepage widget and its admin toggles into `darkmode.deadlines.js`, leaving `darkmode.js` with a smaller bridge/dependency surface and keeping the public manifests/build scripts/docs aligned with the modular runtime layout. | Reload DTU Learn homepage and Settings/Admin tools to verify the Deadlines widget, chevron/refresh controls, and both homepage widget toggles still behave the same after the split. |
| 2026-04-21 | Repo cleanup | Extracted the DTU Learn Lessons bulk-download shell into `darkmode.lessons-bulk.js`, leaving `darkmode.js` with shared download/runtime helpers while the public manifests and build scripts now ship a dedicated Learn-only module for the control lifecycle. | Open a DTU Learn Lessons page and verify the `Download content` control still appears under the search area, opens/closes correctly, and responds to folder selection after the split. |
| 2026-04-21 | Repo cleanup | Moved the full DTU Learn Lessons bulk-download runtime out of `darkmode.js` and into `darkmode.lessons-bulk.js`. The module now owns the control lifecycle, section-tree parsing, legacy/API fallback resolution, file discovery, and bundled/native download flow, while `darkmode.js` keeps only a tiny bridge and shared host-level helpers. | Open a DTU Learn Lessons page and verify the `Download content` control still mounts under the search area, the section list still populates, bundled ZIP/native fallback downloads still run, and legacy/modern lessons trees still resolve folders correctly after the extraction. |
| 2026-04-21 | Repo cleanup | Moved the CampusNet participant-intel host/runtime helper cluster out of `darkmode.js` and into `darkmode.participant-intel-host.js`. The new module now owns CampusNet page detection, semester/course parsing, course-title filtering, and participant-list DOM helpers, while `darkmode.js` keeps only the bridge and the remaining participant-intel orchestration. | Reload CampusNet participant pages, profile pages, and the frontpage, then verify participant-intel widgets still detect the right page types, parse course/semester/program data correctly, and keep Semester Twins/Profile/Retention features working after the extraction. |
| 2026-04-21 | Repo cleanup | Moved the Semester Twins scoring/analytics cluster out of `darkmode.js` and into `darkmode.participant-intel-scoring.js`. The new module now owns overlap scoring, study-line-specific course detection, and match ranking, while `darkmode.js` keeps only the bridge and the remaining host/runtime orchestration. | Reload DTU Learn and the CampusNet frontpage, then verify Semester Twins still ranks matches, respects scope/filter settings, and still suppresses same-study-line-only overlap cases correctly after the extraction. |
| 2026-04-21 | Repo cleanup | Extracted the Study Planner exam-cluster UI shell into `darkmode.studyplan-exams.js`, leaving `darkmode.js` with the exam data parsing and mapping logic while the new module owns the timeline render, editor modal, and grade countdown badges. | Open `studieplan.dtu.dk` and verify the exam timeline, risk summary, editor modal, manual edits, and grade countdown badges still render and update after the split. |
